fun -> int multiply(a, b) {
    result = a * b;
    return result;
}

fun main() {
    x = 5;
    y = 10;
    z = x + y * 2;

    m = multiply(x, y);

    if (m > 50) {
        m = m - 10;
    } else {
        m = m + 10;
    }

    while (m < 100) {
        m = m + 5;
    }
}
